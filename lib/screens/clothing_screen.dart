import 'package:flutter/material.dart';

class ClothingScreen extends StatelessWidget {
  const ClothingScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Clothing Screen'),);
  }
}